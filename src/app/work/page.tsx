import MotionProviders from "@/providers/MotionProviders";
import WorkPage from "@/templates/WorkPage";

const Work = () => {
  return (
    <MotionProviders>
      <WorkPage />
    </MotionProviders>
  );
};

export default Work;
